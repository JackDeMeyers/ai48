{
  "name": "ai48",
  "scripts": {
    "start": "yarn build && yarn go",
    "go": "node dist/main.js",
    "build": "./node_modules/typescript/bin/tsc --target es5 src/main.ts --outDir ./dist/",
    "fresh": "yarn fullclean && yarn install && yarn build",
    "clean": "rm -rf dist",
    "fullclean": "rm -rf dist node_modules"
  },
  "license": "MIT",
  "devDependencies": {
    "readline-sync": "^1.4.9",
    "tslint": "^5.10.0",
    "typescript": "^2.9.1"
  }
}